# Project Agents Registry

This file registers the active AI team members allowed to operate inside this workspace.

- **conductor-agent**: Orchestrates the GitHub issue pipeline using local skills. Defined in `.agents/agents/conductor-agent/agent.json`.
- **code-refinement-agent**: Handles code fixes and test refinements. Defined in `.agents/agents/code-refinement-agent/agent.json`.
